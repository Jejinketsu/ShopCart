import React from "react";
import CreatedListsTemplate from "../components/templates/CreatedLists";

const CreatedLists = () => {
    return <CreatedListsTemplate />;
};

export default CreatedLists;
